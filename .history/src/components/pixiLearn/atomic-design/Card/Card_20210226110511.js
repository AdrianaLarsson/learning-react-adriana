import Card from 'react-bootstrap/Card';
//Packages
import styled from 'styled-components/';

const StyledCard = styled(Card)`
padding: 50px;
margin: 20px;
background-color: rgba(0,0,0,0.05);
box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.200);
border-radius: 4px;
`;

export default StyledCard;